//@ts-check
/**
 * Esta funcion genera una descripcion devolviendo el parametro nombre
 * @param {string} nombre es un parametro en el prototipo de la funcion describir
 * @return {string} devuelve un mensaje prefijado y el valor del parametro nombre
*/

function describir(nombre) {
return “Esta es una figura llamada: “ + nombre;  //ERROR: comillas curvas " "; se deben usar o las comillas rectas " " o las simples ' '
}